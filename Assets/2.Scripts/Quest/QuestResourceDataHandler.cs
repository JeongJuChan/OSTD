using System;
using System.Collections;
using System.Collections.Generic;
using UnityEditor.iOS;
using UnityEngine;

public class QuestResourceDataHandler
{
    private GameData questData;

    public void Init()
    {
        /*questData = Resources.Load<GameData>($"{Consts.GAME_DATA}/QuestData");

        List<SerializableRow> questDataRows = questData.GetDataRows();

        for (int i = 0; i < questDataRows.Count; i++)
        {
            List<string> elements = questDataRows[i].rowData;


        }*/
    }

}
